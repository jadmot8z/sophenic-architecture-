import { LocalAgentWorkspace } from "@/components/agent/local-agent-workspace";

export const metadata = { title: "Agent local — Sophenic" };

export default function AgentPage() {
  return <LocalAgentWorkspace />;
}
